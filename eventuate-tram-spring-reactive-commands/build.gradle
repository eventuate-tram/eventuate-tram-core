
dependencies {
    api project(":eventuate-tram-reactive-commands")
    api project(":eventuate-tram-spring-commands-common")

    api "org.springframework.boot:spring-boot-starter:$springBootVersion"
}
tasks.withType(Test).configureEach {
    useJUnitPlatform()
}
